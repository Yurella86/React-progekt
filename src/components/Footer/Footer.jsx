
import React from "react";
import s from "./Footer.module.css";

const Footer = () => {
    return (
        <div className={`${s.footer} ${s.shadow}`}>
            5
        </div>
    );
}

export default Footer;